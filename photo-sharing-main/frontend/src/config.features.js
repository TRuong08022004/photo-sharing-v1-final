// Fine-grained feature toggles for recently added UI.
// Flip any of these to false to hide the corresponding interface while keeping the backend logic intact.
export const ENABLE_FRIEND_FEATURES = false; // friend list + add/remove friend + friend count
export const ENABLE_LIKE_FEATURE = false; // like button/count on photos
export const ENABLE_PROFILE_EDIT_FEATURE = false; // profile edit form
